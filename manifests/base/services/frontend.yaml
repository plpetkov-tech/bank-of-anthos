apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    application: bank-of-anthos
    tier: web
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: frontend
    application: bank-of-anthos
  type: ClusterIP